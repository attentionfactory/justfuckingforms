// @jff/ui — design tokens + base styles. CSS-only package; this file exists so tsc has an input.
// Add any shared TSX components here in later phases (e.g., a JFF-flavored <Code> wrapper).
export {};
